package com.juniperGMVAD.app;

import java.util.List;

import org.junit.Test;

public class CountryDataTest {
    /*@Test
    public void setValue() {
        Database db = new Database();
        CountryData countryData = new CountryData(Country.USA, db);

        countryData.setValue(Indicator.MVA, 2000, 0d);
    }

    private Database generateDatabase() {
        Database db = new Database();
        db.setYearValue(Country.WLD, Indicator.MVA, 2000, 1d);
        db.setYearValue(Country.WLD, Indicator.MVA, 2000, 1d);
        db.setYearValue(Country.WLD, Indicator.MVA, 2000, 1d);
        db.setYearValue(Country.WLD, Indicator.MVA, 2000, 1d);
        db.setYearValue(Country.WLD, Indicator.MVA, 2000, 1d);
        return db;
    }*/
}
