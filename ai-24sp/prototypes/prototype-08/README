README

If you have any questions ask Griffin or Nathan

We didn't create a gitignore file, so we've pushed uncompressed files. A gitignore still needs to be created.
We copied the code to prototype-08.py
We've lost prototype-08.py. We uploaded too many files to github, so it has omitted 50,000+ files. Sorry
